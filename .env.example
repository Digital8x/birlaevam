# ─── Server Configuration ──────────────────────────────────────
PORT=5555
SESSION_SECRET=birla-evam-super-secret-key-change-this

# ─── Database Configuration (MySQL) ───────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=birla_evam

# ─── Administrative Authentication ────────────────────────────
ADMIN_USER=admin
ADMIN_PASS=BirlaEvam2026

# ─── Fallback SMTP Settings (Managed dynamically in Admin Panel) 
# Note: If database dynamic settings are saved, they will take precedence over these.
SMTP_HOST=localhost
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=

# ─── Tracking / Webhooks ──────────────────────────────────────
# Optional Google Sheets Webhook url for duplicate logging
GOOGLE_SHEET_WEBHOOK=
